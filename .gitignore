# Build outputs
/target/
**/*.rs.bk

# Cargo.lock IS committed: this workspace produces a binary, so reproducible
# builds and supply-chain auditing depend on it. Do not add it here.

# archwarden's own cache directory (when dogfooding).
# Only the cache — .archwarden/ also holds generated artefacts such as
# AGENT_RULES.md, which users may legitimately want to commit.
.archwarden/cache/

# Personal tooling, not required to work on this project
.ai-jail

# Editors
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Env files
.env
.env.local

# Test artefacts
tmp/
*.snap.new

# cargo-mutants scratch output
mutants.out/
mutants.out.old/
